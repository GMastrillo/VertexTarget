import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { KpiCard, PageHeader, SectionTitle, StatusBadge } from "@/components/admin/AdminUI";
import { clients, projects, aiLogs } from "@/lib/admin-data";

export default function AdminOverview() { return <div><PageHeader eyebrow="Workspace / overview" title="Bom dia, Gabriel." description="Aqui está o pulso da VertexTarget hoje. Acompanhe receita, clientes e a operação em tempo real." action={<Link href="/admin/projetos" className="admin-secondary-btn">Ver operação <ArrowUpRight size={16}/></Link>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><KpiCard label="Receita recorrente" value="R$ 42.600" change="+12,8%" icon="revenue"/><KpiCard label="Clientes ativos" value="12" change="+2" icon="users" accent="violet"/><KpiCard label="Projetos em andamento" value="08" change="+1" icon="projects" accent="orange"/><KpiCard label="Execuções IA hoje" value="1.284" change="+24,5%" icon="bot" accent="green"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_1fr]"><div className="admin-card"><SectionTitle title="Projetos recentes" meta="Ver todos →"/><div className="divide-y divide-white/[.06]">{projects.slice(0, 4).map(p => <div key={p.id} className="flex items-center justify-between gap-4 py-3.5 first:pt-0"><div className="min-w-0"><p className="truncate text-sm font-medium">{p.title}</p><p className="mt-1 text-xs text-slate-500">{p.client} · {p.type}</p></div><StatusBadge status={p.stage}/></div>)}</div></div><div className="admin-card"><SectionTitle title="Atividade do AI Lab" meta="Hoje"/><div className="space-y-4">{aiLogs.slice(0, 4).map(log => <div key={log.time} className="flex gap-3"><span className="mt-1 h-2 w-2 shrink-0 rounded-full bg-cyan-300 shadow-[0_0_10px_#00f0ff]"/><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><p className="truncate text-sm">{log.automation}</p><span className="text-[10px] text-slate-600">{log.time}</span></div><p className="mt-1 text-xs text-slate-500">{log.client} · <span className={log.status === "Sucesso" ? "text-emerald-300" : "text-rose-300"}>{log.status}</span></p></div></div>)}</div></div></div><div className="admin-card mt-6"><SectionTitle title="Carteira de clientes" meta={`${clients.length} exibidos`}/><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{clients.map(c => <Link href="/admin/crm" key={c.id} className="rounded-xl border border-white/[.06] bg-white/[.02] p-4 transition hover:border-cyan-300/30"><span className="grid h-9 w-9 place-items-center rounded-lg bg-gradient-to-br from-cyan-400/20 to-violet-500/20 text-xs font-semibold text-cyan-200">{c.initials}</span><p className="mt-3 truncate text-sm font-medium">{c.name}</p><p className="mt-1 truncate text-xs text-slate-500">{c.service}</p></Link>)}</div></div></div>; }
