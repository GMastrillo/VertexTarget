import { Activity, Bot, CheckCircle2, Clock3, Cpu, Zap } from "lucide-react";
import { PageHeader, SectionTitle, StatusBadge } from "@/components/admin/AdminUI";
import { getAiLogs } from "@/lib/admin-repository";

export default async function AILogsPage() {
  const aiLogs = await getAiLogs();
  const successCount = aiLogs.filter((log) => log.status === "Sucesso").length;
  return <div><PageHeader eyebrow="VertexTarget / intelligence layer" title="AI Lab & automações" description="Observabilidade das automações de marketing e chamadas à API Gemini." action={<span className="admin-live-pill"><i /> {aiLogs.length} logs carregados</span>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><div className="admin-card"><div className="flex items-center justify-between"><span className="grid h-9 w-9 place-items-center rounded-xl bg-cyan-400/10 text-cyan-300"><Bot size={18} /></span><span className="text-[10px] text-emerald-300">Dados reais/mock</span></div><p className="mt-4 text-xs text-slate-400">Execuções exibidas</p><p className="mt-1 text-2xl font-semibold">{aiLogs.length}</p></div><div className="admin-card"><div className="flex items-center justify-between"><span className="grid h-9 w-9 place-items-center rounded-xl bg-violet-400/10 text-violet-300"><Zap size={18} /></span><span className="text-[10px] text-emerald-300">{aiLogs.length ? Math.round(successCount / aiLogs.length * 100) : 0}%</span></div><p className="mt-4 text-xs text-slate-400">Taxa de sucesso</p><p className="mt-1 text-2xl font-semibold">{successCount}</p></div><div className="admin-card"><div className="flex items-center justify-between"><span className="grid h-9 w-9 place-items-center rounded-xl bg-orange-400/10 text-orange-300"><Clock3 size={18} /></span><span className="text-[10px] text-slate-500">Gemini API</span></div><p className="mt-4 text-xs text-slate-400">Logs monitorados</p><p className="mt-1 text-2xl font-semibold">{aiLogs.length}</p></div><div className="admin-card"><div className="flex items-center justify-between"><span className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-400/10 text-emerald-300"><Cpu size={18} /></span><span className="text-[10px] text-emerald-300">RLS</span></div><p className="mt-4 text-xs text-slate-400">Origem protegida</p><p className="mt-1 text-2xl font-semibold">Ativa</p></div></div><div className="mt-6 grid gap-6 xl:grid-cols-[.8fr_1.7fr]"><div className="admin-card"><SectionTitle title="Automações ativas" meta="Fonte Supabase" /><div className="space-y-1">{["Qualificação de leads", "Copy para campanhas", "Resumo de atendimento", "Segmentação preditiva", "Reativação de pacientes"].map((name, i) => <div key={name} className="flex items-center gap-3 rounded-lg px-2 py-3 hover:bg-white/[.03]"><span className="relative grid h-8 w-8 place-items-center rounded-lg bg-emerald-400/10 text-emerald-300"><Activity size={15} /><i className="absolute -right-0.5 -top-0.5 h-2 w-2 rounded-full bg-emerald-300" /></span><div className="flex-1"><p className="text-xs font-medium">{name}</p><p className="mt-1 text-[10px] text-slate-600">{i % 2 ? "A cada 30 min" : "Tempo real"}</p></div><CheckCircle2 size={14} className="text-emerald-400" /></div>)}</div></div><div className="admin-card"><SectionTitle title="Logs recentes · Gemini API" meta="Supabase / ai_runs" /><div className="overflow-x-auto"><table className="admin-table"><thead><tr><th>Horário</th><th>Automação / cliente</th><th>Modelo</th><th>Tokens</th><th>Latência</th><th>Status</th></tr></thead><tbody>{aiLogs.map((log) => <tr key={`${log.time}-${log.automation}`}><td className="font-mono text-xs text-slate-500">{log.time}</td><td><p className="text-sm text-slate-200">{log.automation}</p><p className="text-[10px] text-slate-600">{log.client}</p></td><td className="font-mono text-[10px] text-slate-500">{log.model}</td><td>{log.tokens}</td><td>{log.latency}</td><td><StatusBadge status={log.status} /></td></tr>)}</tbody></table></div>{!aiLogs.length && <p className="py-8 text-center text-sm text-slate-500">Nenhum log encontrado.</p>}</div></div></div>;
}
